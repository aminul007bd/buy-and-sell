export interface Listing {
  id: string;
  name: string;
  description: string;
  price: number;
}

export interface LifeTimeSpend {
  spend: string;
  retailSpend: string;
  twelveMonthSpend: string;
  return: string;
  margin: string;
}
